# -*- mode: snippet; require-final-newline: nil -*-
# name: direddo
# key: direddo
# --
(defun --dired-do-$1 ()
  (interactive)
  (mapc
   '(lambda (marked-file)
      ($0))
   (dired-get-marked-files)))